{
  "name": "comint-mode",
  "main": "./lib/comint-mode",
  "version": "0.0.0",
  "description": "Run a command interpreter (AKA a REPL) in a text buffer inside atom.",
  "keywords": [
  ],
  "activationCommands": {
    "atom-workspace": "comint-mode:newShell"
  },
  "repository": "https://github.com/jeffgran/atom-comint-mode",
  "license": "MIT",
  "engines": {
    "atom": ">=1.0.0 <2.0.0"
  },
  "dependencies": {
    "pty.js": "0.3.0",
    "ansi_up": "1.3.0"
  }
}
